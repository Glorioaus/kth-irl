# 仓内迁移与使用

日期：2026-09-17。范围：补完依赖与证据落地，不重新盘点全部历史。

## 日常只使用仓内位置

| 资源 | 仓内位置 | 使用规则 |
|---|---|---|
| 产品规则实现 | `plugin/src/kth_hybrid/kernels/` | 新运行层已有源码，不从旧包导入业务控制器 |
| 180项目录 | `plugin/src/kth_hybrid/data/approved_catalog.v1.json` | 正常应用校验hash后读取，无wheel/子进程依赖 |
| 批准参考原件 | `.local/reference/approved-baseline/oracle.whl` | 仅作来源/机械差分参考，不是正常运行依赖 |
| 批准attached源码 | `.local/reference/approved-baseline/src/kth_irl/` | 81个Python模块，80与wheel一致；唯一部署差异保留，不自动替换批准方法 |
| 原版测试与fixture | `.local/reference/approved-baseline/tests-src/` | 81个Python文件和5个fixture，仅作对应问题参照，不自动执行旧整链 |
| 其他原测试/方法/registry | `.local/reference/approved-baseline/tests/`、`method/`、`registries/` | 旧provenance具名资产的核验副本 |
| 三个Case保护快照 | `.local/reference/project-state/cases/` | `real-pilot-case`、`current-basis-case`、`v12-contract-case`，不可直接打开或改写 |
| 历史候选及资格观察 | `.local/reference/project-state/evidence/` | 保留旧调用身份和时点，不冒充新产生的事实 |
| 已审成功/失败、资产盘点和路线来源 | `.local/reference/project-state/history/` | 由既有审计E编号选择复制，不展开新历史 |

完整逐文件来源、现路径、SHA256和字节数见[仓内迁移索引](仓内迁移索引.csv)。5954个迁移文件包含5728个Case文件，不是5954份独立业务证据；不与前次33项飞书下载数量相加解释能力。

## 身份和保护

批准wheel SHA256：`2c49050858555ebb063a7b82177ee43e7caf7d2b93e2319998d7d0b047a471fe`。

固定目录SHA256：`abb21b0e5ff7aae2bf39f9149f3139ea017347ef1875c885da9a9a9e516d7f04`。保留六个getter完整目录及原code_pointer，计数CRL13/BRL36/TRL25/TMRL38/IPRL32/FRL36；CRL仍仅1–4级，不宣称完整官方方法。

历史源码和新产品源码角色不同。“已经有源码”足以支持把目录数据冻结到新包，不意味着可以从任一历史版本挑选规则替换当前批准方法。

三个Case分别1909/1909/1910个文件，复制含现存WAL/SHM。完成源前后文件集/hash与目标一致性检查；没有打开源数据库。副本内部出现旧路径时保留其历史身份，不字符串替换数据库或封存响应。后续运行从保护快照再复制到`.local/runs/`，所有变更只作用于新的工作副本。

既有成功裁决及日志已按E18/E19保存到`project-state/history/`；失败与最新候选按E20–E34的具名文件保存。历史裁决的hash仍有效，本次目录加载改动另作离线验证，不能宣称新提交已经继承全范围验收。

## 不等于删除许可

本次解除当前工作仓对具名旧wheel/Case位置的使用依赖，没有删除旧分支/工作树，也没有将所有旧工作树独有未提交成果归档。`sunny-skills`还含其他项目，不能整仓删除；`D:\t`不能整树删除。

清理时只针对明确候选核对独有成果、档案与引用，不重新审核业务历史。工作仓自包含是清理的一项前提，不代替删除对象审核。
