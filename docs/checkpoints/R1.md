# R1 检查点（2026-09-08）

分支 `codex/runtime-rebuild`；代码提交 `c4cb646`（T01–T06 共 6 个本地提交）；
授权范围：R0＋R1（T01–T06＋I/O 历史交接、audit/trace、证据 census、R1 交接工件）。
本文件为自评记录，**不构成审核方已批准**。

## 一页结论

**自评：R1 通过（PASS，限 R1 范围）**。理由对照 v3 计划 §6 完整 PASS 条件：

| 要求 | 结果 | 证据 |
|---|---|---|
| 实际功能（存储/台账/审计/恢复） | 达成 | 102 项测试通过；真实进程终止/孤立工件/fencing/outcome_unknown 均有实证 |
| 全量分层盘点 | 达成 | 80 清单捕获逐行；不可读 4 条保留分母；两附件/两 abandonment/簿记单列 |
| 至少一个真实合格窄主张的判据消费 | 达成 | BP 第 4 页市场假设（第一方＋文档自述日期）→ 资格 qualified → CRL1-C1 实际消费；trace 全链通过 |
| trace 反向到封存原文 | 达成 | 两条真实 trace（合格路径＋真实不足路径）；篡改/断链/越界测试可见失败 |
| 困难边界（CRL/FRL/TMRL） | 达成 | CRL 5–9 method_unsupported、CRL 拒原生 insufficient、FRL na_policy 三态、TMRL 身份约束均有独立测试 |
| 恢复 | 达成 | 计数型模拟 Provider 恰好一次派发；崩溃→dispatch_recorded→恢复期保守 outcome_unknown→不盲重发 |

未达成/超出 R1 的（如实列出，不影响 R1 判定）：六维全量规则对照（R2/T07）、
正式 D1（R3）、真实 Provider（未授权，零网络）、22 个来源未完成主张提取。

## 实际能力（新增）

1. **可靠原件仓**：内容寻址 blob（同字节唯一身份）、文件级 os.replace＋fsync、
   读时重核；截断/篡改/目录逃逸可见失败。避开目录级替换（实测确定性 WinError5）。
2. **事务台账**：SQLite WAL；原子认领＋单调 token＋fencing；失败 attempt 留存；
   外部任务 planned→claimed→dispatch_recorded→终态/outcome_unknown。
3. **审计 trace**：判据结果→资格→主张→来源→封存字节反向核验；摘录 hash、
   pdf_page/zip_member 抽取投影重核；断链即 `TraceBroken`。
4. **全量分层盘点**：L0 资产（不可读留分母）／L1 字节／L2 去重（同 hash 组）／
   L3 主张资格／L4 判据可用性；CLI 产出 JSON＋中文存量表。
5. **资格判断**：来源/身份/时间/独立性四类判断各带依据；空 502、搜索摘要类、
   行业综述、晚抓取无发布证明、同 hash 组全部按规则拒绝或转 needs_review。
6. **真实判据入口**：`evaluate_criterion` 产品状态与原生处置分列；
   R1 窄规则（用途类→级别 1 信息性判据）显式声明范围，不冒充 met。
7. **中文 CLI**：`inspect`（核验视图）与 `trace`（追溯原文），标题明确
   "非正式评估报告"。

## 测试与命令（全部退出码 0）

- 全套（最终）：102 通过 / 0 失败 / 0 跳过（带三个显式环境变量，见 handoff.json）
- 分任务：T02 18 项、T03 25 项、T04 26 项（16 合成＋10 真实只读）、T05 12 项、
  T06 21 项（含真实 Case 门控 2 项）
- census CLI 导入重跑幂等（同身份去重，无重复导入）
- 原始日志与 hash：`D:\t\kth-rebuild-cases\weijiu-r1\audit\`

## 关键事实发现

1. **目录级 `os.replace` 到已存在目标在本机确定性失败**（WinError 5，3 次有界
   重试耗尽仍失败；全新短路径亦然）——历史上 22 个测试错误的调用形态。新存储
   不依赖目录整体替换。文件级 replace／SQLite WAL 跨进程重开正常。
2. **4 条清单捕获目录 PermissionError 不可读**（13a964b…、c50df109…、
   fb22cf18…、fb8e46f2…），与上轮审核"4 个不可读"一致；保留在分母，未宣称损坏。
3. **BP 自述主体为"微玖（苏州）光电科技有限公司"**，与评估主体"武汉微玖光电
   科技有限公司"法人实体不同；两者法人关系未核验，已计入该主张 cannot_prove。
4. **36 条非空正文＝18 种 hash**；最大同 hash 组 12 条（同一公司报道被重复捕获）；
   独立来源族未审查，全部按"未知"处理，不以 hash 数当独立证据数。
5. 附件复制登记时间 2026-08-27T04:13:11Z 晚于证据截止 03:02:29Z 约 71 分钟
   （第一方材料按文档自述日期 2026-07-16 资格化，依据已记录）。

## 未解决项（移交 R2）

1. 4 条不可读捕获的实质影响评估（是否值得提运维需求由 Owner 决定）。
2. 来源族/转载关系审查（5 个同 hash 组的独立性判定）。
3. 22 个来源未完成主张提取与资格化（L3 分母明确显示）。
4. CRL 5–9 范围裁定包、逐判据规则对照（T07）。
5. 苏州/武汉主体关系的正式核验（影响第一方材料的主体范围解释）。

## 建议与实测工作量提示

- R2（T07–T09）可按同型任务实测外推：本轮判据目录＋单判据入口＋资格层实现
  约占一个执行段；180 条逐判据规则对照的主要成本在规则追溯与测试矩阵，不在
  基础设施（已就位）。
- 实际耗时：**一个连续执行段，约 50 分钟墙钟**（2026-09-08 00:05–00:54 本地），
  远低于 5 工作日上限；无中断。不以模型 token 数折算人日。
- 下一步等待 Owner/审核方独立验收本交接包；未获授权不启动 R2。
