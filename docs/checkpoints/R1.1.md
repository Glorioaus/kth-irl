# R1.1 检查点（2026-09-08）

分支 `codex/runtime-rebuild`；修复代码提交 `a41559d`（基线：被审 `609b26b`，其间无其他提交）。
授权：仅 R1-01至R1-06 及 P2 更正；不进入 R2/真实Provider/正式D1。
原 R1 Case `D:\t\kth-rebuild-cases\weijiu-r1` 作为被审快照**只读保留**；本阶段全部新运行写 `D:\t\kth-rebuild-cases\weijiu-r1_1`。本文件不构成审核方已批准。

## 一页结论

**自评：PARTIAL（修复范围内 R1-01…R1-06 与 P2 全部完成并有实证；真实正向资格门仍诚实未通过）。**

- 六组 P1 反例：37 项审计反例测试先写入并确认旧代码 **34 项失败**（清单存档 `adversarial-old-code-20260908.log`，sha256 `5cb0730c…`）；修复后 **37/37 通过**。
- 全套测试：**144 通过 / 0 失败 / 0 跳过**（含 2 项真实 Case 门控 + 10 项真实只读盘点）。
- 真实正例按修复后规则重新资格化：BP 主张 **needs_review**（时间证据仅文件名候选级＋文档自识主体为苏州法人未核验）、公司报道 **rejected**（晚抓取无发布证明），CRL1-C1 均如实 **insufficient**。按修复指令预期："若仍只能得到Gap，如实提交PARTIAL，不发明另一条'合格规则'过门"——正向资格门（一条真实合格窄主张被消费）**仍未通过**，负向通道（诚实不足、可追溯、不冒充）已修复并实证。
- 实际耗时：**一个连续执行段约 42 分钟墙钟**（2026-09-08 09:05–09:47 +0800，读指令起）；不据此重估项目总工期。

## 逐项修复对照（R1-01…R1-06 + P2）

| 编号 | 修复位置 | 旧版反例结果 | 新版结果 |
|---|---|---|---|
| R1-01 资格 | `qualification.py` v2（三含义分离/有源CaseBasis/严格日期/白名单）、`store.py`（case_basis 表、document_subject 列、source_time_evidence 追加版本表） | 错主体附件/未来日期/缺日期/文件名冒充正例均 `qualified`（34失败清单含 Q1-Q3 同型） | 8 项 R1-01 反例全过：错主体→身份unknown；未来/缺失/非法日期→fail；文件名→候选unknown；登记晚于截止→fail；无源CaseBasis→ValueError |
| R1-02 判据 | `dimensions.py` v2、`kernels/__init__.py`（IMPLEMENTED_RULES 单判据 CRL1-C1 带 registry 出处；删除 use_class 通用路由；TMRL 结构修复） | 无证据 `native_proposal=met` 写入终值；未登记判据 succeeded；未实现规则误报 insufficient；TMRL 合格输入被误拒 | 6 项反例全过：提案只入 notes；未登记→method_unsupported；未实现→method_unsupported（与不足区分）；CRL1-C1 按有出处规则消费；TMRL 成对测试合格通过/不合格拒绝 |
| R1-03 trace/冻结 | `audit.py` v2（严格 JSON/正向闭包/摘录文本绑定hash）、`runner.py`（完整ID、input_digest、同ID异输入拒绝）、`store.py`（claims.input_digest、runs 留痕） | 空链/畸形JSON/替换摘录文本 trace 仍通过；同ID异输入 replay_consistent=true；ID 截断碰撞 | 8 项反例全过：空链/畸形/闭包不符/摘录文本改写均 TraceBroken；同ID异输入 RuntimeError；完整ID无碰撞；case_basis 持久化；时间证据追加版本保留旧版 |
| R1-04 事务 | `journal.py` v2（BEGIN IMMEDIATE＋条件UPDATE＋行数检查；终态不可变；_reject_reason 区分输入/fencing/终态） | 双认领同时成功（token1,2）；旧worker覆盖新结果；同token二次提交改写；失败翻成功 | 5 项反例全过：barrier 固定交错恰好一个认领；迟到提交拒绝；同token二次提交拒绝且output不变；失败attempt不可翻转 |
| R1-05 恢复 | `journal.takeover_stale_claim`、`runner.CountingSimulatedProvider`（响应先 put_bytes 落盘读回重核再 commit；crash_after_persist_before_commit） | claim后崩溃任务永久卡死；响应不存在却登记succeeded | 4 项反例全过：claimed+零外部动作可受控接管（新attempt更大token，旧worker复活被拒）；已派发/unknown 不允许静默接管；succeeded 可读回响应字节；持久化后提交前崩溃保留孤立工件且不盲重发 |
| R1-06 来源 | `intake.py` v2（CAP::<目录名> 实例级Source共享blob）、`store.capture_dependencies`（逐份封存真实hash） | 同正文不同采集合成一个Source；依赖"齐全"但0份封存 | 4 项反例全过：同正文两实例两Source共享blob；760份依赖逐份封存可读回；隔离副本删除原目录后元数据完整可核验；独立性不从来源数推断（同正文3实例仍unknown） |
| P2 盘点/计量 | `test_real_capture_readonly.py`（分母/分类/声明一致性，兼容76或80可读）、`census.py` L3/L4 计量分离 | 固定36断言在80可读环境失败；claims行数冒充已审数 | 分类闭合+清单声明一致性断言（两环境均成立，历史观测以注释保留）；Claim/已审/各状态/distinct qualified/待提取来源/criterion结果/可用关系分列；N/A与重复消费不增加已资格主张数 |
| P2 I/O结论 | `weijiu-r1_1/audit/io_probe_v2.py`（操作前采样）+ 本文件撤回声明 | R1.md 断言"22次旧I/O错误均因目标目录存在"；`*_exists_before` 操作后采样 | **撤回该断言**（无逐次前态证据；dimension_ports.py:71 替换前显式拒绝已存在目标，两场景不能证明同因）；结论限定"同卷/短路径/NTFS/本机受控条件"；v1探针保留为历史 |
| P2 交接自洽 | 本组文件 | BP trace hash 63位；POSIX命令不可直接粘贴；commit口径混用 | 完整64位hash见 R1.1-handoff.json；PowerShell 可执行命令；代码commit `a41559d` 与含交接文件的 HEAD/统计分别标明 |

## 新 Case（weijiu-r1_1）实测

- 采集实例模型：81 来源 = 76 捕获实例（40空502实例共享零字节blob、公司报道12实例共享718e402e、ed1dcd79×4）＋1附件＋4 zip成员；760 份依赖逐份封存。
- 有源 CaseBasis 入库（主体/截止/依据）；BP 登记文档自识主体=微玖（苏州）法人＋候选级时间证据（追加版本）。
- 两条真实切片幂等复跑（同输入摘要同结果）：BP→needs_review→insufficient；公司报道→rejected→insufficient；trace 全链通过；CLI inspect/trace 退出码0。
- **业务输入变化**：相对 R1 的唯一输入变化是资格/判据规则收紧（v1→v2）与新时间证据登记；主体、截止、证据池未改动。

## 未解决项（如实）

1. **正向资格门未通过**：现有真实素材在诚实规则下无一条可 qualified（时间证据缺口：文件名候选/登记晚于截止/正文无成文日期）。补证途径：文档正文自载成文日期的素材、或 Owner 对附件截止前在池的正式证明、或主体关系核验——均属新业务输入，需另行批准后按新版本登记。
2. 苏州/武汉法人关系未核验（影响第一方解释，已入 cannot_prove）。
3. 4 条不可读捕获（PermissionError）处置待 Owner 决定。
4. 来源族/转载独立性未审（同hash组仍记未知）。
5. 79 个采集实例来源未完成主张提取（inspect 如实显示）。
6. 真实 Provider/模型链路未测（零网络授权，计数型模拟替身如实标记 simulated）。

## 复验命令（PowerShell，可直接粘贴）

```powershell
cd D:\UGit\kth-irl-hybrid\plugin
$env:KTH_CASE_ROOT='D:/t/kth-rebuild-cases/weijiu-r1_1'
$env:KTH_REAL_SESSION_ROOT='D:/t/kth-phase8-real-session-e7edcba32c19969a317f4bca/session'
$env:KTH_REAL_CASE_DIR_1='D:/t/kth-rebuild-cases/weijiu-r1_1'
.\.venv\Scripts\python -m pytest tests\ -q                      # 期望 144 passed
.\.venv\Scripts\python -m kth_hybrid.cli inspect --case-dir D:\t\kth-rebuild-cases\weijiu-r1_1
.\.venv\Scripts\python -m kth_hybrid.cli trace --case-dir D:\t\kth-rebuild-cases\weijiu-r1_1 --result-id 'RESR::CLM-R11-BP-CRL1::CRL1-C1'
# 反例复现（应在当前代码全过；旧代码失败清单见 adversarial-old-code-20260908.log）
.\.venv\Scripts\python -m pytest tests\test_r1_1_adversarial.py -q
```

完成即停止送审；未获新批准不启动 R2、不 merge/push、不生成正式 D1/报告。
