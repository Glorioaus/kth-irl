# 离线角色合同候选

状态：执行方本地自检候选，独立审核待进行；未调用真实模型或Provider。

角色attempt必须绑定完整六维`view_id/input_digest`、同一主体scope及全部六个实际结果身份。
PRO、CON、CHAIR使用不同生产者与上下文；轮次为预先给定的1–3轮，每个回应绑定对手候选。
角色输出禁止包含成熟度、原生处置、总分、投资建议或最终决定。

角色候选只有经匹配的受控确认，才能适配为待写入`dimension_evidence_reviews`的review；确认
不直接产生准则处置。错视图、漏维度、错主体、越权字段、身份不独立或确认错候选均拒绝。

单元测试`4 passed`；真实六维＋汇总＋离线角色综合门控`7 passed`。模拟通过不等于真实模型链
通过，不授权正式D1、报告或投资决定。
