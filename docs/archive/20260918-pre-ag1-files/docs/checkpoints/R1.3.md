# R1.3 检查点（2026-09-08）

分支 `codex/runtime-rebuild`；修复代码提交 `0a83c62`（+清理 `4278dae`）；基线：被审 `3a4f6b4`（核对一致，工作区干净）。
授权：仅 R1.3 限定收口（A/B/C 残留 + 直接相关测试/交接修正）；不进入 R2、不重设架构、不扩大预算。`weijiu-r1/r1_1/r1_2` 与全部旧资产只读保留；新运行全部写 `D:\t\kth-rebuild-cases\weijiu-r1_3`。本文件不构成审核方已批准。

## 一页结论

**自评：R1.3 范围内 A/B/C 残留已收口（待独立复验）。** 25 项新反例先在基线 `3a4f6b4` 复现（20 项失败/3 项接口缺失，日志存档）；修复后 25/25 通过；全套 **195 通过 / 0 失败 / 0 跳过**（含 3 项真实 Case 门控 + 10 项真实只读）。真实政府页正例在新合同下完整成立（见下）。实际耗时：一个连续执行段约 79 分钟（11:41–13:00，读指令起）。

## A/B/C 验收矩阵（入口 / 信任边界 / 原始证明 / 成功发布点 / 故障点）

| 轴 | 代码入口 | 信任边界 | 原始证明 | 成功发布点 | 故障点 |
|---|---|---|---|---|---|
| A 主体引用 | `qualification._resolve_case_field_reference`（case=None→拒） | 主体/别名引用必须解析到**本Case封存**的 provenance blob 字段且值==主体名 | `case:identity-plan.json#/subjects/0/canonical_name_claimed`（blob 94f3402d…，值=微玖（苏州）…，claimed） | 解析成功且值一致（正例 test_a1_positive） | 路径不存在/字段缺失/值不一致→ValueError（A1 复现→修复） |
| A 第一方归属 | `qualification._resolve_locator_text(document_subject_basis)` | 结构化定位（byte_range/pdf_page）且**定位内容含主体名** | BP 第1页投影（结构化）；合成正例正文区间 | 内容含主体名→identity ok（test_a2_positive） | "page 999"字符串/定位内容不含主体→unknown（A2 复现→修复） |
| A 时间入口 | `qualification._judged_from_located_text`（published_at/document_self_date 统一） | published_at 也须 `published_at_locator`；**从定位文本提取实际时间值**比对（精度+时区）；声明≠提取→fail | 政府页正文 `时间：2026-01-31 17:17:00`（byte 定位，钟面按声明时区+08解释） | 提取值==声明且≤截止→ok（GOV 正例） | 同日改凌晨→fail（A3）；仅日期跨截止→unknown（不补时刻）；自报过去时间→fail（A1） |
| A 登记证明 | `qualification._registration_binding_error` + `_resolve_registration_proof` | 记录须绑定**当前原件**（proof.document_sha256 或记录内 document_sha256==source blob） | attachment-inventory 封存记录 | 绑定一致+字段值==声明+≤截止→ok（test_a4_positive） | 绑定另一文档（64×f）→fail（A4 复现→修复） |
| B 语义映射 | `kernels.interpret_claim_for_criterion`（candidate）+`confirm_mapping`（确认+否定门控） | 关键词只做候选；确认须留痕记录；**否定门控**：引文/解释含"尚未识别…"→即使确认也拒绝 | GOV 引文"设计产能将响应300万-400万副AR眼镜市场显示需求"＋SEMANTIC_CONFIRMATION（复核留痕） | candidate+有效确认→confirmed→可 succeeded（B1正例/GOV） | 否定文本→unmapped_negated（B1 复现→修复）；无确认→candidate→insufficient |
| B N/A | `kernels.check_na_legality` v4 + `runner._resolve_na_proposal` | applicability_ref/flag_ref 必须**预解析**到封存对象字段（`_resolved`+值）；case_flags 布尔不再放行 | 封存 financing-policy.json 字段 | 引用解析+flag 值真→合法N/A（test_b2_positive） | 指向不存在文件→拒（B2 复现→修复）；未解析/值假→拒 |
| C 冻结与发布 | `contracts.run_input_digest_v3`（v4扩展）+`audit.verify_result_bindings`+runner 原子发布 | 冻结含 source 判断字段+时间证据**修订快照**+资格内容摘要+映射确认+N/A 全文；发布=**先验证后 INSERT** | weijiu-r1_3 冻结输入（frozen_inputs JSON） | 验证全过→单条 INSERT 发布成功 | 验证中断→无行（C2a）；验证失败→execution_failed 失败候选（C2b）；改时间/N-A依据→摘要变→重放拒（C1/C7）；来源字段/资格状态/映射删除/版本快照（含别名与来源依据）改动→trace断（C3-C6）；追加新时间修订→旧结果不受影响（不误伤） |

## 逐项修复对照（复验编号 → 反例旧/新结果）

| 探针 | 旧（基线复现） | 新 |
|---|---|---|
| A1 主体引用指向缺失文件＋自报过去 published_at | qualified | 主体引用 ValueError；published_at 无定位→time fail（正例：真实封存引用+定位日期通过） |
| A2 文档归 Company-B＋自报 Company-A 第一方"page 999" | qualified（identity ok） | 定位不可解析→identity 非 ok（正例：结构化定位含主体名通过） |
| A3 正文 10:08:25，声明改同日 01:00 | qualified | 声明与提取值不符→fail |
| A4 登记证明绑定另一文档（64×f） | qualified | 绑定核验失败→fail（正例：绑定当前原件通过） |
| B1 "尚未识别任何市场需求…"＋调用方确认 | succeeded/trace ok | unmapped_negated→insufficient（关键词仅候选；正例＋留痕确认通过） |
| B2 N/A 依据/flag 指向不存在文件 | succeeded/trace ok | 引用未解析→拒（正例：封存策略文件解析通过） |
| C1 改 N/A basis 重放 | 接受、摘要不变 | 重放 RuntimeError（摘要含 N/A 全文） |
| C2 trace 入口中断 | 独立连接见 succeeded | 验证先行：中断→无结果行；确定失败→execution_failed（两个确定性测试） |
| C3 来源发布时间改 2099 | trace 通过 | trace 断（source 判断字段 vs 冻结） |
| C4 资格改 rejected | trace 通过 | trace 断（资格状态+内容摘要） |
| C5 删除全部映射记录 | trace 通过 | trace 断（映射记录存在+引文绑定封存原文） |
| C6 版本快照改别名/来源依据 | trace 通过 | trace 断（快照全字段比对） |
| C7 来源时间 7/1→8/1 重放 | 接受、摘要不变 | 重放 RuntimeError（source 字段入摘要） |

**崩溃发布边界**（确定性测试）：(a) 验证入口中断→结果表无该 result_id；(b) 验证返回 broken→落库 execution_failed（rationale 记录失败候选语义，非业务NO）；均以独立连接复查。

## 真实切片（weijiu-r1_3，政府页素材保留）

- **GOV 正例**：qualified（第三方载明主体全名＋正文自载发布时间定位＋单源）→ 映射 confirmed（引文逐字＋否定门控通过＋留痕确认，范围=**来源陈述该假设**）→ CRL1-C1 **succeeded（非原生 met）** → trace 通过。
- BP：needs_review（第一方主体一致；时间证据=文件名候选级）→ insufficient。
- 公司报道：rejected（自载发布 2026-08-27T10:08:25Z 晚于截止 7h06m，真实定位核验后如实不合格）→ insufficient。
- 幂等重跑：业务对象零增长（sources 81 / imports 94 / claims 3 / quals 3 / results 3 / 依赖 380×3 口径）；运行历史 6 条单独计数；CaseBasis 版本 1（内容不变未追加）；时间证据修订 3。

## 交付更正（本轮指令点名项）

1. `test_trace_failed_result_not_published_as_succeeded` 已重写为**确定性**版本：前置断言正向发布成功→验证时刻注入断链（删除资格记录）→断言 trace_ok=false 且不发布 succeeded→独立连接复查。
2. 幂等口径分列：业务对象（不增长）vs 运行历史 runs（允许追加，单独计数 6）vs 版本历史（CaseBasis 1、时间修订 3——真实事件才追加）；identity-plan 重复导入已由 find_import 消除（imports 94，R1.2 为 96）。
3. 阶段末快照自动生成（snapshot-stats-r1_3.json）；R1.2 交接 runs=6 与接收 runs=9 的差异来源已说明：门控测试运行产生的执行记录（运行历史），非业务对象膨胀。
4. `or True` 恒真断言在 R1.2 已随重写删除，本轮复核确认不存在。

## 尚未解决（如实，不包装）

1. 语义确认目前是**留痕的人工/受控复核记录**（executor-r1_3），非自动化语义判定——这是设计边界而非缺陷声明；R1.3 未宣称一般语义理解能力。
2. 主体法人身份仍为 claimed（identity-plan＋政府页一致，未工商核验；零网络）。
3. 78 个来源实例未完成主张提取（含同 hash 重复捕获）；4 条 PermissionError 不可读捕获为技术差异保留（未升级为 Owner 补材料任务）。
4. 180 条判据仍只实现 CRL1-C1（T07/R2 范围）；真实 Provider/模型链路未测（零网络授权）。

## 复验命令（PowerShell）

```powershell
cd D:\UGit\kth-irl-hybrid\plugin
$env:KTH_CASE_ROOT='D:/t/kth-rebuild-cases/weijiu-r1_3'
$env:KTH_REAL_SESSION_ROOT='D:/t/kth-phase8-real-session-e7edcba32c19969a317f4bca/session'
$env:KTH_REAL_CASE_DIR_3='D:/t/kth-rebuild-cases/weijiu-r1_3'
.\.venv\Scripts\python -m pytest tests\ -q                          # 期望 195 passed
.\.venv\Scripts\python -m pytest tests\test_r1_3_adversarial.py -q  # R1.3反例 25 项
.\.venv\Scripts\python -m kth_hybrid.cli trace --case-dir D:\t\kth-rebuild-cases\weijiu-r1_3 --result-id 'RESR::CLM-R13-GOV-CRL1::CRL1-C1'
.\.venv\Scripts\python D:\t\kth-rebuild-cases\weijiu-r1_3\audit\build-case-r1_3.py   # 幂等复跑
```

完成即停止送审；未获新批准不启动 R2、不 merge/push、不生成正式 D1/报告。
