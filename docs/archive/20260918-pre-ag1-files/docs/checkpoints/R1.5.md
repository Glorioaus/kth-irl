# R1.5 检查点（2026-09-08）

分支：`codex/runtime-rebuild`。本轮仅执行 R1.5 的四项证明关系限定修复，未进入 R2，
未重做 R1.4 已关闭的事务、唯一 CaseBasis、原件核验、N/A 派生字段隔离或受控映射设计。
以下是执行方自检；独立审核尚未执行，不能视为 R1 通过裁决。

## 基线与提交

- 开工基线：`cb622ea282fc97906235478be1c7b9cec0465931`，工作区干净。
- 实现提交：`bebc2a6 修复(R1.5)：限定证明关系与持久化合同`。
- 全套运行时导入：`D:\UGit\kth-irl-hybrid\plugin\src\kth_hybrid\__init__.py`；
  测试 HEAD 为 `bebc2a6`，真实门控切换至新 `weijiu-r1_5`。
- 旧 R1.4、R1.3 及更早 Case、旧 session 均只读；未 reset、未联网、未读取凭证、
  未调用真实 Provider/模型 API、未改 ACL/注册表、未 push/merge/tag/release。

## 四项限定修复与五个反例

1. **关系不可拼接**：字段绑定现在包含原件、字段和值之外的逻辑记录作用域。
   N/A 的 applicability/flag/subject 必须来自同一封存政策记录作用域；第一方的 subject 与
   document_sha256 也必须来自同一归属记录作用域。Company-B 政策加 Company-A 身份字段、
   Company-A 身份字段加 Company-B 归属 hash 均被拒绝；同主体、同记录、严格 bool 的 N/A
   正例仍通过。
2. **时区规则有源且适用**：无偏移钟面换算要求 `timezone_basis` 和
   `timezone_scope_ref` 两个封存字段引用，规则值必须一致、两个字段必须属于同一规则记录，
   且来源族必须匹配当前 Source。`case:missing-rule.json` 不能再放行；显式偏移换算保留。
3. **第一方证明可持久化**：`document_subject_basis` 从 SQLite 读取时严格 JSON 解码；
   非法 JSON 为不成立证明。合法归属证明经关闭/重开数据库、runner、受控 CRL1-C1 映射、
   trace 和同输入重放均通过。
4. **实际使用的证明进入闭包**：独立 aliases 记录必须同时绑定其主体字段；runner 冻结
   主体、别名、第一方、时区和 N/A 的实际关系证明，发布前与 trace 同样重建。删除实际消费的
   aliases provenance 后 trace 失败。

R1.5 新反例在当前接口先复现为 `5 failed`，修复后
`tests/test_r1_5_proof_relations.py` 为 `5 passed`。该集合包括：
`B1_spliced_na_subject`、`A1_unresolved_timezone_basis`、
`A2_first_party_splice`、`A3_persisted_first_party_positive`、
`C2_alias_proof_removed`。

关联回归：`118 passed, 3 skipped`；三项跳过均为当时未设置新真实 Case 门控变量，
未用 skip 代替真实验证。最终新 Case 全套无 skip。

## 真实 Case

新 Case：`D:\t\kth-rebuild-cases\weijiu-r1_5`。

| 切片 | 资格 | 映射 | 产品状态 | trace |
|---|---|---|---|---|
| GOV | qualified | confirmed | succeeded | 通过 |
| BP | needs_review | candidate | insufficient | 通过 |
| 公司报道 | rejected | unmapped | insufficient | 通过 |

- GOV：封存 identity-plan 的 aliases 与主体字段同属 `/subjects/0`；页面钟面
  `2026-01-31 17:17:00` 仅按 Case 内登记、适用于 `gov-agency` 的 `+08:00` 规则换算为
  `2026-01-31T09:17:00Z`。该成功只表示来源陈述市场需求假设，不是原生 met，也不证明
  市场规模、产能或成熟度。
- BP：仍未登记 `document_subject` 归属证明，且日期仅为文件名候选；不能描述为“只差日期”。
- 公司报道：当前拒绝理由为精确发布时间声明与所选定位仅含日期不一致；没有将其误写成已由
  当前定位算出晚于截止。

## 验证与快照

- 真实门控、inspect、三条 trace：`3 passed, 1 warning, exit 0`。
- 全套：`219 passed, 0 failed, 0 errors, 0 skipped, 1 warning, exit 0`。
  唯一 warning 是 `zipfile` 重复成员拒绝测试触发的 Python UserWarning，测试本身通过。
- 建库首次运行因脚本漏读 `bp_data` 退出 1；原始失败保留在建库日志。修正后在同一新 Case
  幂等重跑 exit 0，未覆盖该失败记录。
- 全套后快照：业务对象 `sources=85`、`imports=91`、`claims=3`、`qualifications=3`、
  `criterion_results=3`、依赖 `400/400/400`；运行历史 `runs=9`；版本历史
  `CaseBasis=1`、时间证据修订 `3`、映射复核 `1`。

完整命令、退出码、日志路径和 64 位 SHA-256 位于 `R1.5-handoff.json`；脱敏摘要位于
`R1.5-evidence-summary.md`。

## 停止条件与未完成项

本轮到此停止送审，不自动进入 R2。未完成范围不变：仅 CRL1-C1 已实现；其余 179 条判据、
六维、真实 Provider/模型、正式 D1、报告和工商核验均未实现或未授权。R1.5 不宣称独立审核
通过。
