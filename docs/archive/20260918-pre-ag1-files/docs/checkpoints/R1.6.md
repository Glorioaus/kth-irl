# R1.6 检查点（2026-09-08）

分支：`codex/runtime-rebuild`。本轮仅执行 R1.6 双点修复：统一字段路径解析/关系作用域，
以及统一合法别名第一方资格、证明收集、冻结与 trace。未进入 R2，未重开 R1.4/R1.5 已通过的
事务、存储、持久化、时区规则或受控映射设计。以下仅为执行方自检，独立审核尚未执行。

## 基线与提交

- 开工基线：`7ec57de89a43a11a0ee2db2c10ff14fbaf1cb639`，工作区干净。
- 实现提交：`91359fc 修复(R1.6)：统一路径解析与别名归属证明`。
- 全套实际导入：`D:\UGit\kth-irl-hybrid\plugin\src\kth_hybrid\__init__.py`；
  全套测试 HEAD 为 `91359fc`，真实门控使用新 `weijiu-r1_6`。

## 双点修复

1. **统一路径合同**：`/`、`.`、`[n]` 均由一个解析器转换为实际解析得到的 token 序列。
   字段取值、逻辑记录作用域、冻结 binding 与 trace 重核共同使用同一规范路径/record token。
   同文件内 `a`/`b`、`rows[0]`/`rows[1]` 是不同关系；不再以错误的根 `/` 作用域拼接。
2. **别名第一方一致性**：CaseBasis 冻结有效主体集合；别名记录必须同一逻辑记录绑定规范主体。
   第一方归属证明以本次实际匹配的规范名或已核验别名解析，并冻结
   `matched_subject`、匹配类型、归属字段和原件关系。证明收集失败不再仅写错误字符串后继续
   发布成功。

## 反例与配对验证

R1.6 新用例初次运行 `10` 项中 `4 failed`：

- `P1_dot_cross_record_bypass`：不再允许成功 N/A。
- `P1_bracket_cross_record_bypass`：不再允许成功 N/A。
- `P1_dot_first_party_bypass`：不再 qualified/succeeded。
- `P2_alias_owner_proof_not_frozen`：合法别名第一方的实际归属证明已被冻结，删除后 trace 失败。

斜杠跨记录控制样本本来已拒绝，保留。以下合法配对均通过：

- 同一记录的 slash、dot、bracket N/A 路径分别成功。
- slash/dot/bracket 跨记录 N/A 分别不成功。
- 规范名与合法别名第一方均可关闭/重开数据库，经 runner、完整 trace 和同输入重放成功。
- 删除规范名或别名的 ownership 证明、删除独立 alias 证明均出现可见 trace 失败。

`tests/test_r1_6_path_and_alias.py` 修复后为 `10 passed`；关联回归为
`128 passed, 3 skipped`，三项 skip 仅发生在未设置新真实 Case 变量的局部运行。

## 真实 Case 与全套

新 Case：`D:\t\kth-rebuild-cases\weijiu-r1_6`。

| 切片 | 资格 | 映射 | 产品状态 | trace |
|---|---|---|---|---|
| GOV | qualified | confirmed | succeeded | 通过 |
| BP | needs_review | candidate | insufficient | 通过 |
| 公司报道 | rejected | unmapped | insufficient | 通过 |

- GOV：identity-plan 的 `subjects/0` 规范名与 aliases 使用同一规范路径关系；Case 内
  `gov-agency` 的 `+08:00` 规则将页面钟面解释为 `2026-01-31T09:17:00Z`。成功只支持
  “来源陈述市场需求假设”，原生处置仍为 null。
- BP：仍缺归属登记且文件名日期仅候选。
- 公司报道：当前拒绝原因仍为精确时间声明与日期级定位不一致，不误称已由定位算出晚于截止。

真实门控、inspect、三条 trace：`3 passed, 1 warning, exit 0`。
全套：`229 passed, 0 failed, 0 errors, 0 skipped, 1 warning, exit 0`。唯一 warning 是
zip 重复成员拒绝测试触发的 Python `UserWarning`，测试本身通过。

## 最终快照与保护

- 测试后新 Case：业务对象 `sources=85`、`imports=87`、`claims=3`、
  `qualifications=3`、`criterion_results=3`、依赖 `400/400/400`；运行历史 `runs=9`；
  版本历史 `CaseBasis=1`、时间证据 `3`、映射复核 `1`。
- R1.5 数据库 SHA-256 仍为
  `020ee2ab5241bb933c79454c0c36b2840257ee87de49fecf181cdccc09dee9f9`；旧 session.json
  仍为 `dc3e31b086b9fdb9857a9179478b469791f458e35c1a3cf24b74776a28b49375`。
- 原始命令、退出码、日志路径及 64 位 hash 见 `R1.6-handoff.json`；脱敏摘要见
  `R1.6-evidence-summary.md`。

## 停止条件

完成 R1.6 后停止送审，不自动进入 R2。仍未完成或未授权：除 CRL1-C1 外的 179 条判据、
六维、真实 Provider/模型、正式 D1、报告和主体工商核验。R1.6 不宣称独立审核通过。
