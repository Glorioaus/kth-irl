# R1.4 检查点（2026-09-08）

分支：`codex/runtime-rebuild`。本轮只执行 R1.4 原 A/B/C 残留修复，不进入 R2，
不重设架构、不生成正式 D1/报告、不 push/merge/tag/release。独立审核尚未执行，以下
仅为执行方自检交接。

## 基线与范围

- 开工基线：`43803441330298019238636e7a6895f756bc9638`，工作区干净。
- 新 Case：`D:\t\kth-rebuild-cases\weijiu-r1_4`；`weijiu-r1` 至 `weijiu-r1_3`
  及旧 session 只读。
- 全套测试执行时的实现 HEAD：`87359c9`；实际包导入为
  `D:\UGit\kth-irl-hybrid\plugin\src\kth_hybrid\__init__.py`，不是审核目录快照。
- 本轮提交：
  - C：`f056908 修复(R1.4-C)：统一有效依据与原子发布`
  - A：`5fd6005 修复(R1.4-A)：封存时间与主体证明边界`
  - B：`21179d5 修复(R1.4-B)：封存复核与受限N/A边界`
  - A 真实时区补充：`87359c9 补充(R1.4-A)：支持有源时区规则换算`

## C：有效输入与原子发布

- C1：Case 已有依据后，调用方改截止为 2099 年或改别名会以
  `有效 CaseBasis 与调用方输入不一致` 拒绝；资格、冻结摘要、trace 使用同一存储版本。
- C2：发布前核验已重核原件 hash/长度、定位、摘录、资格、映射、闭包与输入摘要；源长度
  损坏不能发布 `succeeded`，保留 `execution_failed` 候选，非业务 NO。
- C3：验证到 INSERT 处于 SQLite `BEGIN IMMEDIATE` 事务。确定性第二连接写资格被锁定，
  未发生失效变更时结果可正常发布；已在事务前发生的变化由完整核验拒绝。
- C4：冻结主体 provenance 的原件路径、blob hash、字段路径和值 hash；删除该导入记录后
  trace 失败。

当前接口反例的首次红灯为 C1-C4 共 4 失败；新增 R1.4-C 测试修复后为 `5 passed`。
关联回归为 `44 passed, 3 skipped`（当时未设置真实 Case 门控变量）。

## A：封存事实、时区与主体

- A1/A2：带显式偏移的原文按真实 UTC instant 比较：
  `2026-08-26T23:30:00-08:00` 被拒，
  `2026-08-27T09:00:00+08:00` 可通过；Z、日期级、截止边界均有覆盖。
- 无偏移钟面只有在 `timezone_rule` 与非空 `timezone_basis` 均被记录时才换算；
  政府页 `2026-01-31 17:17:00` 按 `+08:00` 规则记录为
  `2026-01-31T09:17:00+00:00`。
- A3：封存登记记录自身必须绑定当前原件；外层 `document_sha256` 仅是预期值，不能覆盖
  记录中的另一文档 hash。
- A4：第一方归属需要封存记录同时绑定主体字段与当前原件 hash；正文只提及主体不再放行。
- A5：别名仅取同一封存身份源实际解析出的列表；调用方追加别名不参与主体匹配。

当前接口反例首次为 5 失败；A 主测试修复后 `7 passed`，政府页有源时区补充后 `8 passed`。
资格与 R1.3 关联回归为 `47 passed`。

## B：候选与核验边界

- B1：`semantic_confirmation` 的裸布尔、confirmator、review_basis 不再确认映射。
  新增狭义 `mapping_reviews` 台账；运行期只消费绑定 CaseBasis 版本、Claim、判据、引文 hash、
  支持范围、decision、reviewer 与依据的记录。该记录是受控离线复核，未冒用 Owner 或独立审核方。
- B2：运行器会删除调用方传入的 `applicability_resolved`、`flag_resolved`、`subject_resolved`，
  再从封存字段重建；缺引用无法形成合法 N/A。
- B3：受限 FRL N/A 只接受当前主体、受控枚举 `no_external_financing` 与严格布尔 `true`；
  Company-B 或字符串 `"false"` 均被拒。
- trace 会重核受控映射记录和 N/A 三个封存字段，不能只相信冻结 JSON。

当前接口反例首次为 3 失败；R1.4-B 正反例为 `6 passed`。受影响的既有回归迁移后为
`97 passed, 3 skipped`（未设置真实 Case 门控变量）。

## 真实 Case 与最终验证

- GOV：`qualified → confirmed → CRL1-C1 succeeded`，原生处置仍为 `null`；支持范围仅为
  “来源陈述该市场需求假设”。
- BP：`needs_review → insufficient`，时间仍只有文件名候选。
- 公司报道：`rejected → insufficient`，自载发布时间晚于截止。
- 三条真实 trace、inspect 与真实门控：`3 passed, 1 warning, exit 0`。
- 全套：`214 passed, 0 failed, 0 errors, 0 skipped, 1 warning, exit 0`。

全套警告来自 `tests/test_intake.py::test_zip_duplicate_names_rejected` 的 Python `zipfile`
重复成员警告；该测试通过，非业务/运行失败。

## 保护、快照与未完成项

- 旧 R1.3 数据库 SHA-256 仍为
  `ce5801022cd550e049b60634b88ec921eefe9814f11a77b12b2f181485048a99`；旧 session.json
  仍为 `dc3e31b086b9fdb9857a9179478b469791f458e35c1a3cf24b74776a28b49375`。
- 全套测试后新 Case 快照：业务对象 `sources=85`、`imports=90`、`claims=3`、
  `qualifications=3`、`criterion_results=3`、依赖 `400/400/400`；运行历史 `runs=12`；
  版本历史 `CaseBasis=1`、时间证据修订 `3`、映射复核 `1`。
- 未完成范围保持不变：只实现 CRL1-C1；180 条规则、六维、真实 Provider/模型、正式 D1、
  报告与主体工商核验均未实现或未授权。R1.4 不宣称独立审核通过。

完整命令、退出码、日志路径和 SHA-256 见 `R1.4-handoff.json`；脱敏证据摘要见
`R1.4-evidence-summary.md`。完成即停止送审，不自动进入 R2。
