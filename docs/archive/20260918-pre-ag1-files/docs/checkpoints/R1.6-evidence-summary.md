# R1.6 证据存量摘要（脱敏）

> 核验记录，非正式评估报告。原文、数据库和原始日志均在仓库外
> `D:\t\kth-rebuild-cases\weijiu-r1_6\audit\`。

## 证明路径

- 所有支持的字段语法 `/`、`.`、`[n]` 在读取、关系校验、冻结与 trace 中规范为相同 token。
  同一文件中不同键和数组成员不再共享关系作用域。
- 有效别名 `Vijo Microelectronics`、`微玖光电` 与规范名均绑定 identity-plan 的同一
  `/subjects/0` 记录；第一方证明记录冻结本次实际匹配的规范名或别名。
- GOV 时区规则 `+08:00` 与 `source_family=gov-agency` 绑定于同一封存规则对象；时区和
  适用域在 trace 时重新解析。

## 测试后计数

| 项 | 计数 |
|---|---:|
| Source | 85 |
| ImportRecord | 87 |
| Claim / Qualification / CriterionResult | 3 / 3 / 3 |
| 依赖行 / 逻辑依赖 / 不同依赖 blob | 400 / 400 / 400 |
| CaseBasis 版本 | 1 |
| 时间证据修订 | 3 |
| 映射复核 | 1 |
| 运行历史 | 9 |

## 真实切片

| Claim | 状态 | 限定 |
|---|---|---|
| CLM-R16-GOV-CRL1 | qualified / confirmed / succeeded | 仅来源陈述市场需求假设；原生处置 null |
| CLM-R16-BP-CRL1 | needs_review / candidate / insufficient | 缺归属登记，文件名日期仅候选 |
| CLM-R16-CAP-CRL1 | rejected / unmapped / insufficient | 精确时间声明与当前日期级定位不一致 |

本表不把来源实例、依赖行或单个 GOV 正例转写为完整证据池、完整六维或正式投资结论。
