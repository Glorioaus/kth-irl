# R1.2 检查点（2026-09-08）

分支 `codex/runtime-rebuild`；修复代码提交 `75846c4`（基线：被审 `84cb1fc`，其间无其他提交，工作区起点干净）。
授权：仅 R1.2 残留修复（复验 P1-A/B/C + P2 接管/测试/统计 + 有限存量核查）；不进入 R2/真实Provider/正式D1。
`weijiu-r1`、`weijiu-r1_1` 与全部旧资产只读保留；本阶段全部新运行写 `D:\t\kth-rebuild-cases\weijiu-r1_2`。本文件不构成审核方已批准。

## 一页结论（两项分开报告）

**① 软件残留是否关闭：已关闭（自评，待独立复验）。** 23 项 R1.2 反例先写入并确认旧代码 **20 项失败**（清单存档）；修复后 23/23 通过；全套 **170 通过 / 0 失败 / 0 跳过**（含 3 项真实 Case 门控 + 10 项真实只读）。复验 P1-A（依据可核验）、P1-B（判据身份与映射）、P1-C（冻结与trace）、P2（接管 attempt、恒真断言、统计口径）全部有反例级实证。

**② 真实正向资格门是否通过：通过（首次）。** 有限核查在**现有素材内**找到一条可核验主体/时点的窄主张，无需 Owner 补证：

> 政府页主张（`RESR::CLM-R12-GOV-CRL1::CRL1-C1`）：苏州工业园区政府网（sipac.gov.cn，封存捕获 8bd0fcdb…）正文**自载发布时间 2026-01-31 17:17:00**（真实可解析 byte 定位，早于截止 2026-08-27T03:02:29Z）＋正文载明主体全名"微玖（苏州）光电科技有限公司"＋市场需求陈述（"设计产能将响应300万-400万副AR眼镜市场显示需求…车载ADB…光通讯等多种应用场景"）。资格 **qualified**（第三方载明事实/时间成立/单源），引文映射成立（引文逐字位于封存摘录），被 **CRL1-C1 实际消费 → succeeded**（非原生 met），trace 全链通过。

另两条真实切片如实不足：BP=needs_review（第一方主体一致但时间证据仅文件名候选级）、公司报道=rejected（自载发布 2026-08-27T10:08:25Z **晚于截止 7 小时 06 分**，真实定位核验后如实不合格——不凑正例）。

## 逐项修复对照（复验编号）

| 编号 | 修复位置 | 旧版反例结果 | 新版结果 |
|---|---|---|---|
| P1-A 依据可核验 | `qualification.py` v3（`_resolve_locator_text`/`_resolve_registration_proof`/`_parse_subject_source_basis`/`_date_text_variants`） | 空第一方依据→身份ok；"第999页"定位→qualified；过去日期+空basis登记→qualified（residual-observations Q1-Q3 同型复现） | 空依据→unknown；定位不可解析/内容不含日期→fail；登记时间须封存记录（blob+字段路径）核验且与声明一致；真实定位+内容含日期→ok（正常正例能过） |
| P1-A 主体依据 | `store.set_case_basis`＋新 Case 构建脚本 | "武汉微玖…"被描述为"Owner 交接/开工指令钉定"，但该名在所指材料中无字段级来源 | **主体修正**为 `identity-plan.json#/subjects/0/canonical_name_claimed` = 微玖（苏州）光电科技有限公司（**claimed 状态**，未工商核验；该文件已封存入 Case blobs）；撤回"武汉=Owner钉定"表述；不更换截止 |
| P1-B 判据身份 | `runner.resolve_criterion`（可信 catalog 按 ID 解析）＋`dimensions`（与 catalog 规范判据逐字段比对） | 同ID改 dimension/level/text 仍 succeeded（复现）；runner 默认自报批准集合，虚构判据+伪造N/A可 succeeded | runner 只接受 criterion_id+catalog，未登记→ValueError；同ID篡改任一字段→method_unsupported 拒绝；自报批准集合路径已删除 |
| P1-B 判据映射 | `kernels.interpret_claim_for_criterion`＋`claim_criterion_mappings` 表＋`dimensions` 映射必要条件 | "办公室墙是蓝色"仍可消费 CRL1-C1（用途交集放行） | 映射三层必要：资格×用途交集×**引文映射**（引文逐字位于封存摘录区间＋保守语义过滤命中留痕）；无关主张→映射unmapped→insufficient；编造引文→unmapped留痕 |
| P1-B N/A 有源 | `kernels.check_na_legality` v3（结构化提案）＋`audit`（na_basis 校验） | N/A 凭布尔 flag+rationale 放行，空链 succeeded 且 trace 失败仍保存成功 | N/A 提案须 `{proposal,basis,case_flag_source}` 且 flag 自身带来源；na_basis 落库并纳入 trace 链校验；无源 N/A 一律拒绝 |
| P1-C 完整冻结 | `contracts.run_input_digest_v3`＋`claims.content_digest`＋`criterion_results.frozen_inputs/input_digest` | 同ID换判据维度/级别/文本→摘要不变可重放（复现） | 摘要含规范判据全文/catalog哈希/批准集/CaseBasis快照/flags/资格策略版本/主张内容摘要/映射快照；任一改动→摘要变→拒绝重放 |
| P1-C CaseBasis 版本 | `store` v3（`case_basis_versions` 追加不可变＋`set_case_basis` 版本化） | `INSERT OR REPLACE` 抹版本，改主体/截止后旧结果 trace 仍按最新行通过（复现） | 版本快照追加保留；结果绑定 `case_basis_version`；trace 按绑定版本校验，新版本不影响旧结果 |
| P1-C 解释篡改 | `audit`（content_digest 重算比对） | 改写已存解释文本，trace 仍通过（复现，含真实Case副本） | 解释/主体/定位/摘录任一改动→content_digest 失配→TraceBroken |
| P1-C 追溯门 | `runner`（trace 失败不发布成功） | trace_ok=false 仍保存/返回 succeeded（复现） | succeeded 但 trace 失败→落库 `execution_failed` 失败候选（不发布成功、不转业务NO），返回 trace_ok=false |
| P2 接管审计 | `journal` v3（`takeover_events` 表＋attempt outcome='claimed'） | 接管后派发/提交，最新 attempt 停留 takeover（复现） | 接管为独立事件留痕；attempt 状态推进到终态（succeeded），事件与状态分离 |
| P2 恒真断言 | `test_r1_1_adversarial.py` | `test_generic_level_routing_removed` 含 `or True` 恒真 | 恒真删除；R1.2 新增无关主张/篡改判据真实反例（见上） |
| P2 统计口径 | `store.dependency_stats`＋`intake` 导入幂等＋`snapshot-stats-r1_2.json` 自动生成 | 依赖1520行/380项/380blob 混报760；重跑膨胀导入 | 三个口径分列（380/380/380，幂等重跑不增）；阶段末快照自动计数（96导入/81来源/3主张/6执行记录/2 CaseBasis版本） |

## 真实素材有限核查记录（范围：去重非空正文18种＋两附件＋封存元数据）

- 18 种去重非空正文中**仅 2 种提及主体**：公司报道（718e402e，自载发布 2026-08-27T10:08:25Z 晚于截止7h→时间不合格）与政府页（8bd0fcdb，自载发布 2026-01-31→合格，即上述正例）。
- BP 与 4 个访谈 docx 正文均无成文/发布日期（文件名日期仅候选）→ BP=needs_review、访谈未强行资格化。
- 其余 16 种（专利/技术综述正文）不提及主体，按"行业综述不能证明主体主张"不构成主体范围候选。
- 公司报道锚点区间[33267,33542)实测为"微玖光电成立于2023年，总部位于苏州工业园区…"——与 identity-plan 苏州主体一致（R1/R1.1 将其描述为武汉法人名载体是不准确的，已随主体修正一并纠正）。
- 扫描记录：`D:\t\kth-rebuild-cases\candidate-scan-r1_2.txt`（含各正文日期模式与主体提及位置）。
- **结论：R1.1 交接中"现有素材无一条可qualified"的表述撤回**——当时只审了 2 条 Claim，不得外推全池；本轮有限核查即在池内找到 1 条合格窄主张。仍余 78 个采集实例来源未做主张提取（含重复捕获），不在此阶段完成。

## 尚未解决（如实）

1. 主体法人身份仍为 **claimed**（identity-plan 声称＋政府页载明一致，但未工商核验）——R1.2 未做外部核验（零网络）。
2. 语义过滤是保守词族命中（留痕可复审），**语义确认仍待人工/方法审查**；succeeded 不冒充原生 met。
3. 78 个来源实例未完成主张提取（含同hash重复捕获，不等于78份独立待审正文）。
4. 4 条 PermissionError 不可读捕获处置待 Owner；来源族独立性未审（同hash组记未知）。
5. 180 条判据仅 CRL1-C1 有实现规则（R2/T07 范围）；真实 Provider/模型链路未测（零网络授权）。

## 复验命令（PowerShell）

```powershell
cd D:\UGit\kth-irl-hybrid\plugin
$env:KTH_CASE_ROOT='D:/t/kth-rebuild-cases/weijiu-r1_2'
$env:KTH_REAL_SESSION_ROOT='D:/t/kth-phase8-real-session-e7edcba32c19969a317f4bca/session'
$env:KTH_REAL_CASE_DIR_2='D:/t/kth-rebuild-cases/weijiu-r1_2'
.\.venv\Scripts\python -m pytest tests\ -q                          # 期望 170 passed
.\.venv\Scripts\python -m pytest tests\test_r1_2_adversarial.py -q  # R1.2反例 23 项
.\.venv\Scripts\python -m kth_hybrid.cli inspect --case-dir D:\t\kth-rebuild-cases\weijiu-r1_2
.\.venv\Scripts\python -m kth_hybrid.cli trace --case-dir D:\t\kth-rebuild-cases\weijiu-r1_2 --result-id 'RESR::CLM-R12-GOV-CRL1::CRL1-C1'
# 幂等复跑（同输入同结果、导入/依赖不膨胀）
.\.venv\Scripts\python D:\t\kth-rebuild-cases\weijiu-r1_2\audit\build-case-r1_2.py
```

实际耗时：一个连续执行段约 57 分钟墙钟（2026-09-08 10:31–11:28 +0800，读指令起；含真实素材核查与两次 Case 构建）。不据此重估项目总工期。完成即停止送审；未获新批准不启动 R2、不 merge/push、不生成正式 D1/报告。
