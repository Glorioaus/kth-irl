# KTH N1：真实可检查窄闭环完整执行输入

版本：N1-plan-v4，2026-09-17自包含工作仓版。与同目录阶段计划配套；此文件可完整交给新执行对话。

## 0. 先判断授权，不从文档推定开工

Owner已采用“保留当前仓库、按合同分模块替换”，停止扩大历史盘点、暂不清理、不另建实现仓，并要求完成工作入口及旧wheel/Case依赖收口。**收口包含必要加载代码改动与离线验证，但未批准N1业务实现或真实调用。**

如果Owner只让你审核本输入：保持只读、只给审核意见。

只有Owner明确要求采用本阶段计划并开工时，才执行下面的P1–P3离线范围及同合同内的必要修复。Owner一次开工确认后，不在合同、模块、接线、测试之间逐小步再请示。P1–P3做完必须交独立审核；没有真实调用新授权不得继续P4。

本输入取代旧W1单模块执行输入。**纯模块、模拟链或测试通过不是阶段终点。**阶段终点是同一产品路径上真实候选、实际专业复核、诚实输出和人工检查；独立工程审核、新外部授权与人工签审不得由执行者替代。

## 1. 唯一入口、事实与首读范围

唯一CURRENT：`D:\UGit\kth-irl-hybrid\docs\CURRENT.md`。你是获准开工后的唯一编辑责任人。仓外旧CURRENT仅是失效跳转提示，不更新它，不维护第二份当前进度。

开工按顺序读取：

1. 上述CURRENT。
2. `D:\UGit\kth-irl-hybrid\AGENTS.md`及适用上层指令。
3. `D:\UGit\kth-irl-hybrid\docs\plans\N1\真实可检查窄闭环阶段计划.md`全文。这是本输入的范围、接口、验收和停止合同。
4. `D:\UGit\kth-irl-hybrid\docs\decisions\runtime-v3.md`。旧七万行不是新运行宿主，业务不变量仍有效。

不通读全部历史，也不再扫描`D:\k7`～`k11`、`kc`、`kth-test`、所有worktree或资产盘点。遇到本阶段具体问题才读取对应证据。资料解释入口为`D:\UGit\kth-irl-hybrid\docs\reference\README.md`，其中更正文优先于首次整理的旧结论；不是另一个执行入口。

已知Git身份：

- 实现仓`D:\UGit\kth-irl-hybrid`，分支`codex/runtime-rebuild`。
- `f13cfc1fa8ced4743501e67dd13500f417206d66`是自包含改动前基线，本次之后的真实HEAD以本仓Git和CURRENT为准，不以旧hash要求回退。开工核对分支、提交和状态，保留新收口成果。
- 已独立通过的本地代码`c6f7b64bc996b2a88322e0bfda731ee47583b7ca`，交接`444d9061ec829a00026691c114c6385af6b55f7e`。未受影响能力保持原范围通过。
- `f13cfc1`包含后续预算/时间修复，但仍为执行候选；214/6是执行方历史日志，不是此HEAD独立审核通过。
- 原始完整目标保留；本阶段不做完整六维、D1/D2、研究报告、CEO Brief、安装发布或投资结论。

## 2. 数据、范围与已知路径

真实业务范围固定：一个Case、一个候选评估单元、最多3个主张、两个准则`TRL5-C1`与`TRL6-C2`。`TRL3-C2`只作离线反例，不进入新真实任务分母。冻结范围早于新候选输出，不根据结果改题。

现有材料：

- 仓内BP原件：`D:\UGit\kth-irl-hybrid\.local\reference\samples\weijiu\inputs\微玖光电BP260716.pdf`。
- 仓内已有四份访谈：`D:\UGit\kth-irl-hybrid\.local\reference\samples\weijiu\inputs\Pre-A高管及客户访谈.zip`。
- BP SHA256：`d2d8a79d6b5202e6b15f7db44c5b14db6125ee025f880e510beab6d2df4284e8`。
- 页17 quote SHA256：`7c51b717e15f617c144252c0dd9ff54871f76e313c4af2bca08161ac61bdfd96`。
- 旧候选文件的保护快照：`D:\UGit\kth-irl-hybrid\.local\reference\project-state\evidence\proposal-response.json`。
- 只读观察快照：`D:\UGit\kth-irl-hybrid\.local\reference\project-state\evidence\只读核查证据.json`。
- 原试点Case的保护快照：`D:\UGit\kth-irl-hybrid\.local\reference\project-state\cases\real-pilot-case`。
- 当前基准Case的保护快照：`D:\UGit\kth-irl-hybrid\.local\reference\project-state\cases\current-basis-case`。
- 时间合同候选Case的保护快照：`D:\UGit\kth-irl-hybrid\.local\reference\project-state\cases\v12-contract-case`。
- 批准wheel参考副本：`D:\UGit\kth-irl-hybrid\.local\reference\approved-baseline\oracle.whl`，SHA256`2c49050858555ebb063a7b82177ee43e7caf7d2b93e2319998d7d0b047a471fe`；正常运行不读取它。
- 正常catalog工件：`D:\UGit\kth-irl-hybrid\plugin\src\kth_hybrid\data\approved_catalog.v1.json`，SHA256`abb21b0e5ff7aae2bf39f9149f3139ea017347ef1875c885da9a9a9e516d7f04`。使用`load_approved_catalog()`；无参`build_catalog_from_wheel()`仅兼容转到固定数据。
- 批准源码/原测试：`.local/reference/approved-baseline/src/kth_irl/`与`tests-src/`；只作参考，不将其加入产品运行路径，不替换六维内核。

以上路径均在本仓。每份副本的旧路径、hash和归属在`docs/reference/仓内迁移索引.csv`；旧路径仅表示来源，不是要求你回旧仓取数据。仓内历史快照自身也不直接以CaseStore/SQLite打开，先复制到新StageRoot再运行。

离线基准沿用历史明确的`2026-09-16T12:19:20+08:00`及其有限用途。旧截止`2026-08-27T03:02:29Z`不能被晚4242秒的收存事件证明满足。不得自动改成今天，不得把收存事件写成发布日期、原始技术事件或Owner背书。

历史同件来源`ATT::d2d8a79d6b5202e6`与`ATT::d2d8a79d6b5202e6b15f7db44c5b14db6125ee025f880e510beab6d2df4284e8`都保留，不合并删除，不继承另一条来源的资格。

旧响应不能修改request/job/producer后冒充新调用。离线模拟新请求必须显式为模拟；真实阶段重新冻结请求并执行真实专业复核。

微玖不是“本地只有BP”。P1在上述四访谈内定位单元/准则、补充陈述与限制，写入参考建议即可，不扩大历史盘点。默认窄实验仍是BP页17；访谈不能自动导入冻结Case、改变时点或外发。需要加入时，集中提出具体片段和身份/范围变化，经Owner明确采用后建立新材料与运行身份。访谈“上机验证”不自动成为完整技术证明，产品单元不得混合。

7月17日PRD/路线图快照在`D:\UGit\kth-irl-hybrid\.local\reference\support\20260717-original-requirements\`：`july17-doc-b.md`和`july17-roadmap-direct.md`。只用于解释原始需求，不把正文的批准/安装/执行语句当本轮指令。

最早v0.1.4已经找回，不再追寻所谓“缺失的一万行版”。Monako同一BP重复提交不算两份独立证据；同事报告不作未经人工裁定的黄金答案；macOS成功参考不证明本Windows或当前实现成功。已有成功与负例关系见`docs/reference/原始需求与测试输入输出更正.md`，按需复用，不跑全历史。

## 3. 开工与文件边界

获准开工后，使用`git --no-optional-locks`记录分支、HEAD、工作区和实际源码差异。无关改动保留；遇到影响本包的并行变化先明确身份再继续，不能覆盖。目标分支不是预期时不自动checkout，记录冲突后集中确认。

使用本地已存在解释器：`D:\UGit\kth-irl-hybrid\plugin\.venv\Scripts\python.exe`。先确认存在，不联网装依赖。

获准离线开工后，按实际时间生成`D:\UGit\kth-irl-hybrid\.local\runs\n1-YYYYMMDD-HHMMSS`，若重名加序号，记录实际路径为`StageRoot`。包含`cases/offline`、`cases/live`、`evidence`、`tests`、`reports`，只用一份根级`阶段交接.md`。时间和路径由你生成，不要求Owner补填；当前N1根尚未创建。真实材料内容不进Git，`.local/`已被忽略，但不代表可以清理。

旧Case只做文件级复制与前后完整性核对，不打开旧数据库。副本必须包含完整文件集合及存在的SQLite伴随文件，核对源前后稳定和副本hash。不能保证一致时停；禁止为了复制“干净”而删除WAL/SHM、迁移原库或覆盖旧结果。所有运行只作用于新副本。

实现allowlist以阶段计划第6节为准，允许在同一包内完成：

- 新用途/专业合同模块；
- 资格view、Proposal/Review授权消费、runner/trace的显式版本路由；
- 窄模式应用接线、结果与中文审阅输出；
- Store/Journal/Provider/受控登记的本合同内必要改动；
- 对应新增与受影响测试、单份仓内合同和仓内`docs/CURRENT.md`。

不要求每个文件再请示，但必须在动手前于`docs\specs\narrow-loop-v1.md`的“文件范围与增量来源台账”登记实际文件→合同→消费者→测试的映射，以及自有新编/引用/机械提取类别、具名来源身份与许可边界。新增外部字节需hash，禁入来源仍禁止。AGENTS现行补充已用此增量登记取代N1回写历史manifest的要求，输出hash在完成后写入StageRoot交接清单，合同不自引用hash。遇到allowlist外需求先用允许的薄适配解决，确实无法实现才集中说明；禁止按目录任意扩改。

不修改kernels业务规则、AGENTS历史正文/检查点、历史provenance、原始主仓、旧worktree、rev744、family-runs、环境凭据。AGENTS顶部2026-09-17补充已取代下方过期的仓外新Case规则。禁止reset/checkout/clean/prune/gc、移动清理、merge/push/tag/release、新实现仓/worktree和安装依赖。未来开工若未另禁本地提交，可仅提交本包明确文件作为审核候选；不用`git add -A`混入他人成果，不能把本地提交当审核通过。

旧wheel运行依赖已解除，不能再把“回旧仓找wheel”作为默认准备。N1使用包内固定目录数据并保留原方法身份；缺件/hash不符时停止并按仓内备份恢复，不绕过校验、不重选同名版本。已有隔离包验证不等于所有宿主、解析器或整产品已验收；当前实际Python环境前提保留。

## 4. 连续执行P1–P3，不以中间模块结束

采用`superpowers:executing-plans`和测试先行方法；技能默认的全历史检查、自动worktree或发布流程与本输入冲突时，以本输入边界为准。

### P1 合同冻结

在`D:\UGit\kth-irl-hybrid\docs\specs\narrow-loop-v1.md`一次写清阶段计划C1–C5的字段、实际函数签名和旧读新写版本：

- 精确NarrowRunSpec及两个准则的请求/返回硬边界；
- 冻结资格观察→用途许可，保留原总状态与UNKNOWN；
- 实际读取证明的可信来源，消费者禁止接受自报`verified=true`；
- 复核`succeeded + supports/refutes/insufficient`与`blocked/failed + null`分离；
- 每条finding的定位、证据强度、配置/环境/结果/阈值与限制；
- 请求级预算、历史scope、旧用量、未知结果与新授权绑定。

为BP两个活跃准则建立人工参考草案：发展路径不等于已完成相关环境测试；送样不等于接近运行规格。不要把模型或执行者草案写为专家已签。缺实际专家不阻断P1–P3，但阻断真实阶段G2。

将四访谈中已定位的相关陈述与限制单列为补充材料建议，未获采用前不混入默认实验。真实正向参考是否足够仍需资格与人工确认，不能因找到文件就改写成已证明正向能力。

完成后立即继续P2，不请求单模块验收。

### P2 实现并接入应用路径

先固定失败测试，再实现最小变化，完成全部消费者接线：

1. `qualification_policy`只裁定用途，资格观察由既有受控读取重建。只解除已证明同件重复对受限自述的错误全局阻断，不解除其他未知或不合格项。
2. 许可绑定实际Claim、criterion、scope、CaseBasis、关系证明及版本。Proposal、Review、runner和trace统一读时核验；不能仅在生产入口跳过`qualified`门而其他消费者仍按旧合同。
3. `review_contract`让prompt、返回校验与内核适配共享完整字段。材料未知有明确缺项可以是insufficient；模型漏字段/非法引用是执行失败。缺料不送入旧`does_not_support`路径。
4. `narrow_review`只做现有组件编排，`narrow_report`只投影封存事实。接入同一CLI显式窄模式，不通过Case本地脚本直接填库制造成功。
5. 冻结新窄profile而不弱化原六维manifest；只输出两个准则和实际未评估范围，不制造全维等级或六维完成。
6. 同时闭合预算/时间最新候选：关联scope在预留事务内计算，最终dispatch可见；跨库继承明确；未知历史用量不归零；请求上界/费用无证明即拒绝。
7. 默认禁网络，普通run只推进本地阶段；真实派发唯一入口校验G1审核放行记录、G2新授权及运行预算，离线测试只注入传输替身。

模块完成后继续P3，不以“代码写完但未接线”结束。

### P3 离线端到端及审核包

新增测试源码文件必须全部落入以下六项，并补充真正受影响的既有测试；运行产物、日志及临时数据放在StageRoot，不把测试源码放进`.local/`：

- `plugin/tests/test_qualification_policy.py`
- `plugin/tests/test_narrow_review_contract.py`
- `plugin/tests/test_narrow_loop_e2e.py`
- `plugin/tests/test_narrow_loop_recovery.py`
- `plugin/tests/test_narrow_budget_gate.py`
- `plugin/tests/test_narrow_report.py`

必须实际运行以下验收矩阵：

| 场景 | 结果 |
|---|---|
| 真BP新副本，当前已明确基准，同件重复，自述用途 | 经真实产品入口走到模拟专业复核及可读不足/反证结果；不是实际模型验收 |
| 合成支持/反证/缺料/执行失败 | 四类结果不混写；合成身份显式；全部走同一应用代码 |
| 同hash错主体/晚时点/无关系证明；不同hash同生产者/转载 | 不继承资格、不增加独立来源、不越用途 |
| 请求/返回出现范围外准则或第4个主张 | 拒绝或按冻结合同明确范围外，不能悄悄扩大实际任务 |
| 模型漏字段、伪finding、无出处支持 | 合同失败，不输出业务not_met或NO |
| 声明未知且注明实际材料缺项 | 合法insufficient，不虚构指标；保留专业复核身份 |
| 响应已封存后实际进程中断与重启 | 零新transport、输出一致、原attempt不丢 |
| 原件/上游/许可被改写 | 读时拒绝，旧成功不继续暴露为有效结论 |
| 新旧账本、关联scope/授权修订、双连接、跨Case | 无漏算/双算/隐式重置；测试最终dispatch路径 |
| 2项历史未知、无输入上界、过期、越材料、已撤销 | 派发前拒绝，零transport |
| 中文记录、result.json及trace | 同一事实来源，重要语句有定位；历史/模拟/真实来源不混写 |
| 受影响旧接口与恢复回归 | 行为保留；不要求重新审全部未触及历史 |

在`D:\UGit\kth-irl-hybrid\plugin`目录运行新增窄集。使用当前执行中已记录的`$StageRoot`，不是Owner填写变量：

```powershell
$pytestRoot = Join-Path $StageRoot ('tests\pytest-' + [guid]::NewGuid().ToString('N'))
if (Test-Path -LiteralPath $pytestRoot) { throw '测试目录必须全新，不能覆盖' }
& 'D:\UGit\kth-irl-hybrid\plugin\.venv\Scripts\python.exe' -B -m pytest tests/test_qualification_policy.py tests/test_narrow_review_contract.py tests/test_narrow_loop_e2e.py tests/test_narrow_loop_recovery.py tests/test_narrow_budget_gate.py tests/test_narrow_report.py -q -p no:cacheprovider --basetemp $pytestRoot
```

随后运行阶段计划第6节中的受影响既有测试，命令/退出码/日志全部记录。新增命令的CLI回放由实际实现输出为可运行命令，包含真实新路径和精确job/request ID，不能留给Owner补参数。本方案不冒称这些尚未实现的命令现在就存在。

遵守原conftest离线与文件保护；不关闭validator。允许为共享Store/Journal实际变更扩展相关测试，不用“历史验收”跳过受影响回归，也不无因重跑所有旧Case。环境阻断有原始日志，不能改成测试通过。

P3在新工作根输出一份`阶段交接.md`，链接：受审源码身份、合同版本、修改文件、测试原始证据、真实材料副本保护、离线中文审阅记录/result.json、G1审核所需矩阵、完整G2授权建议。更新CURRENT为实际候选状态并送审。

## 5. G1工程门与修复权限

独立审核者重新取得精确候选代码，在独立新副本上验证本阶段关键正反例、端到端、恢复、实际dispatch替身边界、预算和数据保护。只用执行者日志不算独立审核；不得由实现者自签。

只有明确对应代码/合同/任务范围的裁决可放行。离线通过但真实token或计费证明不足，记录`offline_accepted / dispatch_not_cleared`。不要把此状态写成真实窄闭环完成。

G1提出的同合同阻断可由你连续修复再集中复验，不逐个找Owner。新的非阻断改进进同一交接待办。连续两次集中修复/复验仍未闭合的同类阻断，停止追加补丁，提出该模块替换或缩减实验范围；未经Owner批准不自行换路线。

如果没有独立审核能力，停止为待审，交出完整输入与证据，不冒充多模型同意就等于验收。

## 6. G2外部授权：先整理完整方案，不调用

历史3次真实请求、2项用量未知。旧窗口2026-09-16 09:37–13:37 +08:00已经过去，不存在“自动剩余3次”。本次路线选择不延长窗口、不重置账本。

你负责填好已知信息并集中提出：

- 精确代码/合同/任务范围、材料外发片段、CaseBasis、最多3Claim/两个准则。
- 主备配置候选：GLM `glm-5.2` 内部网关和DeepSeek `deepseek-v4-pro`；实际端点由已知当前源码核对后写入建议包。内部网关未有该试点真实成功证据，不能声称已验收。
- 新提案上限：6请求、累计输入60000、累计输出24000（含推理）、合计人民币100元、4小时窗口。它不是已批准额度或费用预测；派发计划必须在真实上界和计费依据下成立。
- 逐mission旧消耗处置、请求级token证明、费用依据、备选/格式纠正次数和停止策略。
- 实际人工审阅人、已冻结的参考标准及盲审安排。未指定就明确状态，不虚构姓名或批准。

Owner只需要一次批准/调整新的真实外发、预算/窗口及专业责任安排。已知路径、hash、模型候选与任务你来整理；确需新材料/时点时作为真正未知项集中提出，不扩大历史扫描。

缺有效计数/价格依据时保持禁调用；需要外部查询，也须在新授权中单列范围，不能把联网查询伪装成本地准备。批准记录具有真实时刻、有效期和适用任务，不接受仅自报`approved=true`。

只有G1相应项通过、G2明确生效且人工参考标准就绪后，才可继续P4。本执行输入单独被采用，默认仍只授权离线P1–P3，不授权P4。

## 7. P4真实执行、G3人工检查与阶段终态

真实阶段获准后，在批准范围内连续完成：

`真实材料 → 新冻结请求的候选 → 按用途资格 → 独立上下文真实专业复核 → 规则适配/明确缺口 → 中文审阅记录与trace → 人工签审`

每次派发检查实际窗口、剩余预算和任务身份；确定性失败只按预授权策略处理；首次外部结果未知立即停链，不换后缀/模型/Case重发。所有失败、原响应、费用/未知量保留。

人工审阅每个重要主张：原文定位、计划与完成区别、配置/环境/指标匹配、自述与独立证明区别、缺料与反证区别、错误/失败诚实性。关键无来源支持、错主体/时点升权或隐瞒失败，任何一项阻断；不能用平均分抵消。另一模型只能辅助，不能代签实际审阅人。

人工签审绑定实际材料、结果摘要、规则版本、身份和时间。参考答案不喂给被测模型；旧候选和独立复核过程分开保存。最终独立核对只确认本阶段代码、授权、调用及签审身份一致，不重新审全部历史。

阶段完成必须同时满足：

1. 已有实际真实候选和真实专业复核，不是只等待资格或导入人工响应。
2. 两准则、全部活跃Claim有实际结果或合法局部缺口，无隐藏失败/漏项。
3. 中文记录可人工检查，重要陈述可追溯；恢复不新增调用，保护旧数据。
4. G1/G2/G3成立，费用与未知量处置闭合，实际输出不越权。

全部结果为不足也可记`complete_limited`，但必须已经过真实专业复核和人工判定；同时记`positive_quality=unverified`，不能证明正向专业能力或据此扩成完整产品。资格全部阻断时是`material_blocked`，不算完成。

其余终态按计划记录：`offline_accepted_waiting`、`engineering_failed`、`quality_failed`、`stopped_by_owner_or_budget`。专业质量失败时建议人工主审、模型只做提取；工程同类失败时替换该模块；需要新材料只请求具体缺项。任何转向、第二次真实实验或新仓建议都须Owner明确采用。

## 8. 维护与最终交接

在P1/P2工程检查点、P3送审、G1裁决、G2授权变化、真实运行终态和G3签审时更新唯一CURRENT；不每个小动作发报告。记录精确受审实现身份、实际运行工作根、当前任务、通过/候选/未测和下一动作。

你只能更新执行候选与引用真实裁决。独立审核者负责工程/阶段裁决，实际专业审阅者负责质量，Owner负责业务/外发授权，不能互相代签。

阶段停止或完成时输出短摘要和`阶段交接.md`、中文审阅记录、实际证据链接。**不要自动进入完整六维、D1/D2、报告发布、资产清理或另建仓库。**

## 文档状态补记（2026-09-18）

本输入已停止作为执行入口，以上正文保留其历史身份、行序和原授权边界，不随AG1更新。后续只从[CURRENT](../../CURRENT.md)读取实际权限，并使用[AG1执行输入](../AG1/新执行对话完整输入.md)。不得凭本文件恢复N1开工、旧窗口或人工验收时序。
