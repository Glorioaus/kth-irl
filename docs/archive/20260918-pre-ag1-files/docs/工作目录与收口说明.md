# 工作目录与收口说明

日期：2026-09-17。前次完成文档入口；Owner随后明确要求补完自包含，本版登记必要加载代码改动、具名迁移与离线验证。不是N1业务实现、G1或真实调用。

## 1. 一个仓库，一个当前入口

后续只需打开`D:\UGit\kth-irl-hybrid`，从[CURRENT](CURRENT.md)继续。

```text
README.md                     入口导航，不维护第二份进度
AGENTS.md                     工作纪律和当前授权优先关系
docs/CURRENT.md               唯一可变状态与下一动作
docs/plans/N1/                现行阶段计划、完整执行输入
docs/specs/                  已有工程合同；N1合同获准实施后新增
docs/reference/              需求/版本/样本结论与来源索引
docs/checkpoints/            历史检查点，保留原有身份与结论
baseline/、provenance/        既有方法和来源基线，不因资料收集改权威
plugin/                      唯一产品实现，本次不改
.local/reference/            私有原件、聊天证据、可读视图和审计快照
.local/reference/approved-baseline/ 批准源码、原测试、wheel和方法参考，不是旧运行宿主
.local/reference/project-state/     三个Case、历史候选/观察、成功与失败裁决的保护快照
.local/runs/n1-实际时间/       以后获准产生的新Case副本、运行、报告和测试产物
.local/tmp/                  以后按需产生的临时文件
.local/workspace-closure/    本次收口前快照和核验结果
.local/backups/              本机可恢复校验档案，不是异地灾备
```

`.local/`整体被Git忽略。它只是本地数据区，不是另一个仓库；不进Git不表示可删除。换机器时仅clone仓库不会得到原件、Case和私有证据，须同时带上校验过的私有资料归档；本机归档不能抵御整盘故障。

## 2. 生效关系与历史保护

当前Owner明确指令优先；不冲突的安全与业务不变量保留。AGENTS顶部现行补充取代其中过期的当前状态和仓外新Case位置规则，其下历史原文不删除。日期较新的文件不自动拥有方法批准或外发授权。

| 旧位置 | 本次处置 |
|---|---|
| `D:\t\kth-project-reset-20260916\CURRENT.md` | 改为停止维护的跳转提示，原全文已保存仓内快照 |
| 仓外`stage-20260917-102225`的阶段计划和执行输入 | 复制到仓内`docs/plans/N1/`后修订；旧文件仅增加已取代提示，正文保留 |
| 仓外`audit-20260917-095217`的W1执行输入 | 仅增加已取代提示，不再发送执行；原文快照保留 |
| 原审计报告与62项证据索引 | 原位置字节不变；仓内`.local/reference/project-audit/20260917-095217/`增加逐字节快照 |
| 旧Case、旧代码/版本包、失败与成功日志 | 不移动、不删除、不覆盖；本次未打开旧Case数据库 |

收口前8份待修改文档快照及SHA256清单位于`.local/workspace-closure/20260917-134732/before-manifest.csv`；80份受跟踪产品文件的保护清单为同目录`plugin-before.csv`。快照及旧文档只用于核对历史，不能凭其中命令启动工作。

现行计划已纳入资料更正：早期PRD/路线图强调用户可完成的判断；微玖四访谈作为已有补充材料池；Monako重复BP不增加独立性；同事报告与macOS影子成功日志仅作受限参考。不是再次全面重审历史。

## 3. 已落实的仓内依赖

正常应用不再从旧目录获取wheel。源码、方法和Case按下表区分用途，不把旧来源身份当成运行定位符：

| 对象 | 现行仓内位置 | 使用边界 |
|---|---|---|
| 正常catalog | `plugin/src/kth_hybrid/data/approved_catalog.v1.json` | 从批准wheel机械冻结，固定hash校验；不手写判据、不启动子进程 |
| 批准wheel/81模块源码/原测试 | `.local/reference/approved-baseline/` | 只作来源与对照，产品不导入旧controller |
| 历史候选/资格观察 | `.local/reference/project-state/evidence/` | 保留历史身份；不重标为新响应 |
| 历史试点Case | `.local/reference/project-state/cases/real-pilot-case` | 保护快照；从这里复制新的工作Case |
| 当前CaseBasis来源 | `.local/reference/project-state/cases/current-basis-case` | 保护快照；P1沿用已明确时点，不自动改日期 |
| 时间合同反例来源 | `.local/reference/project-state/cases/v12-contract-case` | 保护快照；仅相关测试按需取用 |
| 历史成功/失败裁决、盘点与路线 | `.local/reference/project-state/history/` | 由既有审计具名选择复制，不重新扩大盘点 |

完整迁移表见[仓内迁移与使用](reference/仓内迁移与使用.md)及其逐文件索引。三个Case的5728文件已核对源前后与副本hash，包含现存WAL/SHM；原数据库未打开。旧路径保留在历史字节和来源列，不再作为现行输入中的取件路径。

仓内保护快照也不直接运行，只从它再复制新工作Case。现行N1计划/输入已更新到v4，新可写业务根仍仅在`.local/runs/`。没有重新冻结业务日期、放行UNKNOWN或改变方法。

## 4. 验证与停止

前次文档收口范围及快照保留。本次按Owner新指令修改catalog、两个消费者、census默认位置、包数据配置及换行属性；新增隔离/完整性测试，六维kernel、资格/预算业务规则未改。实际allowlist与来源见[自包含合同](specs/workspace-self-contained-v1.md)。

本次证据目录为`.local/workspace-closure/20260917-144447/`：`case-copy-checks.json`记录原Case保护，`catalog-freeze.json`记录固定目录来源，`regression.log`记录相关回归，`final-green.log`记录最终目录测试，`independence.json`记录独立包与Case隔离验证，`交接核验.json`记录文件/备份/Git检查。

已运行相关回归185通过、19跳过；跳过项依赖未指定的历史真实Case环境，不为补数访问原库。最后浅层目录修正的catalog/隔离测试16通过。测试环境有1项已存在的importlib元数据弃用警告；不是产品断言失败。

包构建首次被Miniforge的setuptools附带文本读取权限阻断，原日志保留；随后使用已安装的本地Python 3.12.13/setuptools 84构建，无网络安装。运行验证仍使用项目解释器。验证是同机异地包布局和旧路径拒绝，不宣称已验证所有宿主/新机器安装或完整解析能力。

最终独立包在禁止旧目录、参考wheel、网络及后续子进程的条件下，三个验证Case均通过SQLite完整性和CLI inspect；477/475/478个内容寻址对象逐个重核，18个资格输入视图重建无读取/绑定错误。此项只证明搬迁后的读取与合同重建，不把既有UNKNOWN或业务不足升级为专业通过。

私有资料归档：`.local/backups/workspace-20260917-144447/private-reference.zip`，6318个文件、156110120字节；SHA256为`c34fa6c0dc9fe4567e28a884eb82bd3a19aff6726cc2963b3b2fefba2693ac92`。所有成员实际解压到内存并与源hash比对，源前后身份一致，清单同目录保存。没有把活动环境凭据放进普通归档；历史原包仍以原字节封存。该归档在同盘，不是异地备份，也不包含旧工作树未提交成果的完整归档。

前次独立读者检查两处文档歧义已闭合；本次独立窄代码审核提出浅层安装导入错误，已新增失败测试、修正并由审核者独立重跑2项通过，原阻断已关闭。记录为本次证据目录`独立窄审核记录.md`，不是N1/G1或人工专业签审。

完成后停在可交给新对话的自包含工作仓。N1需要明确离线开工；旧分支/工作树的删除仍须具名清单和独有成果保护。本次不调用Provider/飞书、不读凭据、不清理旧资产、不推送Git。

## 文档状态补记（2026-09-18）

以上是2026-09-17收口的历史记录，保留原目录描述、验证结论与行序，不再维护其中N1“现行/下一步”文字。实际产品方向、权限和阶段见[CURRENT](CURRENT.md)，文档分类见[文档导航](README.md)。目录自包含不等于产品交付，也不授予删除旧资产的权限。
