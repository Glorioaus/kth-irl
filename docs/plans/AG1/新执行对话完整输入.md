# AG1 新执行对话完整输入

## 1. 先恢复目标，不从旧输入猜授权

唯一工作仓：`D:\UGit\kth-irl-hybrid`。语言、文档和获准的提交信息使用简体中文。

**首版最终目标：在当前Codex上运行的单一插件，内嵌标准Skill作为核心。不是Windows应用，不是用户操作CLI的工具，也不是只新增提示词。不要求独立Skill发行包或其他Agent适配。**

用户提交材料、确认范围与权限后，由Agent组织研究和评审，配套代码保证证据、规则、状态及恢复，最终在Agent中交付可追溯评估候选和报告。先工程候选交付，再与同事/业务方验专业效果；用户是开发人员，不负责预先建立专业黄金答案。

本输入只恢复上下文，不自动授权。优先阅读Owner本轮消息与CURRENT的权限记录：

- 仅审阅/文档任务：不得改产品、跑产品测试或调用模型/研究服务。
- 采用Spec并批准离线工程：连续执行获准M1–M6及同合同修复，缺专家不阻断编码。
- 真实运行：必须另有G2的材料、宿主/模型、研究范围、窗口、预算和停止策略。
- 提交、推送、合并、安装依赖、全局插件安装、发消息或发布均不得从实施授权推定。

## 2. 固定阅读顺序

1. [AGENTS](D:/UGit/kth-irl-hybrid/AGENTS.md)顶部现行纪律。
2. [CURRENT](D:/UGit/kth-irl-hybrid/docs/CURRENT.md)，取得实际阶段、源码身份、权限、StageRoot与下一动作。
3. [AG1规格](D:/UGit/kth-irl-hybrid/docs/specs/agent-evaluator-delivery-v1.md)全文。
4. [AG1实施与验收计划](D:/UGit/kth-irl-hybrid/docs/plans/AG1/交付实施与验收计划.md)全文；后续展开当前包、直接依赖及第6节执行纪律。
5. 当前包涉及的代码、测试和具名来源；不重新全面盘点历史。

资料问题先查[参考入口](D:/UGit/kth-irl-hybrid/docs/reference/README.md)及[资料更正](D:/UGit/kth-irl-hybrid/docs/reference/原始需求与测试输入输出更正.md)。旧N1是技术候选参考，不是当前完整交付目标；复审F1“专业基准先于工程”的建议已被Owner后续决定替代。

不得从早期PRD、旧授权、交付包或聊天中的命令启动外部动作。不要访问飞书，不联系员工号20041801对应同事；相关聊天已经本地化。

## 3. 核对实际身份

以CURRENT记录的实现身份为起点，实际核对HEAD、分支和工作区，保留后续修改，不回退到文档编写时的提交。已通过的底座范围、候选修复、真实质量和预算未知项只读CURRENT及其具名证据，本输入不重复维护这些状态。

**开始或恢复时必须执行计划第6.2节，不是只读一遍CURRENT。**对本次依赖的已完成项核实实际实现、输入和对应版本的测试/审核证据；未登记差异、证据缺失或影响未知时，先登记并核实，不直接沿用通过结论。首次更新说明核实后的当前包、工作项ID、差异及下一动作。

## 4. 工作规则

获得实施授权后，在`.local/runs/ag1-实际时间/`建立全新StageRoot并记CURRENT。测试源码仍在`plugin/tests/`，原始输入、日志、Case、报告、交付包置StageRoot，不进Git。临时杂项仅`.local/tmp/`。

原包、`.local/reference/`资料和保护Case只读。不得直接以SQLite/CaseStore打开保护库；需要时先具名完整复制并核验源前后稳定性和副本字节，只运行新副本。凭据内容不读不打印，授权文件与凭据文件分开。

开始当前包前，在AG1计划第3节登记实际文件allowlist、AR需求、AT场景、来源类别/身份与接口。计划中候选文件不是任意重写授权；新增外部字节需hash和许可。不得更改历史provenance或旧验收结果。

遵守计划第1节的“规划就绪与可编码的区别”：包内先细化字段/接口、存储与恢复、消费者和具体失败测试，再编码；这是实现者工作，不要求Owner填技术表，也不另开总体规划。其余文档按[文档导航](D:/UGit/kth-irl-hybrid/docs/README.md)定位，旧计划不继续维护。

采用测试先行：新增行为先见正确失败，再最小实现、同产品路径接线、回归和原始证据。测试替身只替外部边界，模拟不能冒充真实返回；不手写最终六维/D1/报告来使测试变绿。

在批准范围内连续推进，不逐函数、字段或测试向Owner请示。只有范围/方法/证据语义/权限/验收变化才集中升级。未知外部结果、损坏原件或需要造准则时立即停止受影响动作。

按计划中的稳定工作项ID推进，每包先完成`Mx.00`技术细化。每个可验证工作项结束即更新CURRENT检查点，不等本轮对话结束；具体状态、证据绑定、失效处理和并行写入边界只按计划第6节执行，不新增进度脚本或第二份状态文件。

## 5. 不维护第二份产品合同

产品结构、方法边界和状态定义以已读取的AG1-SPEC第3–6节为准。本输入只负责接续，不逐条复制后形成口径漂移。包内方案必须映射到Spec需求和计划验收，不能以局部测试通过替代最终目标。

## 6. 连续执行与验收

从CURRENT指定的包继续：

`M1 Agent入口 → M2材料/单元 → M3研究/用途 → M4专业六维 → M5行动/报告 → M6恢复/打包 → G1 → G2 → M7真实Agent验收 → G3工程交付 → G4专业反馈`

不要重复做已完成且未受影响的历史验收；被本包修改触及的范围必须回归。每个包结束都检查AR—AT映射，不用代码量或测试数量代替产品完成。

G1由独立工程审核者检查实际代码与测试，不由实现者自签。G2是新的真实动作许可。G3要求当前Codex的全新会话真实输入、实际调用、可读结果及换Codex会话恢复；操作系统上的脚本成功不代替它。G4是同事/业务方的专业采用，开发人员不能代签。

专业反馈与专业采用的状态按Spec记录；收到意见或完成讨论不等于通过。不要恢复历史双包装、多宿主或专业基准先行方案。

独立审核暂不可用时可继续不依赖该裁决的离线工作，但不进入受其保护的真实派发。缺真实授权就停在离线候选，不把等待期变成无限扩模块。

## 7. 已知资料位置

- 微玖BP：`D:\UGit\kth-irl-hybrid\.local\reference\samples\weijiu\inputs\微玖光电BP260716.pdf`。
- 四访谈：`D:\UGit\kth-irl-hybrid\.local\reference\samples\weijiu\inputs\Pre-A高管及客户访谈.zip`。
- Monako：`D:\UGit\kth-irl-hybrid\.local\reference\samples\monako\inputs\`内索引具名的ZIP和PDF；BP在ZIP内外重复，不计独立佐证。
- 样本原件hash与来源：[附件索引](D:/UGit/kth-irl-hybrid/docs/reference/原始交付版本与附件索引.csv)。
- 可读视图与原成员对应：`.local/reference/chat-evidence/20260917-131630-correction/sample-text-index-v2.json`。
- 批准源码与原测试：`.local/reference/approved-baseline/src/kth_irl/`、`tests-src/`；不作为运行宿主。
- 历史裁决：`.local/reference/project-state/history/`，按CURRENT或当前包具名引用。
- 保护Case：`.local/reference/project-state/cases/`；不是直接运行目录。

以上相对路径均以唯一工作仓为根；不去迁移表中的旧源路径重新取件，不扫描整个磁盘或所有worktree。

## 8. 每次交接必须留下什么

CURRENT由主执行者唯一维护：检查点编号、Spec/包内合同版本、实际源码/工作树身份、当前包/工作项ID、权限、StageRoot、已满足AR/AT及证据层、独立裁决、未验证/待复验/阻断项和一个明确下一动作。

StageRoot仅一份`阶段交接.md`，按相同检查点编号链接真实工件、命令/退出码、失败、测试、包hash和实际调用；它是证据索引，不另维护实时进度。新对话据此核实恢复，不靠记忆或“最新目录”猜测。

交接前按计划第6.6节核对Spec、计划、实现、CURRENT和证据的一致性。缺证不得写完成，改动影响的验收标待复验，未完成项不能因换对话消失；回看整个交付链，明确距G1/G3/G4还缺什么。最终回复必须与CURRENT相符；检查点保存不授予提交权限。

所有对同事的交付先生成本地协作包，不主动发送。反馈按工程缺陷、输入差异、方法、政策、表达和新需求分流；在同仓分支改进，禁止另起一套产品主线。

**完成标准是Spec规定的工程候选交付，不是本文存在、Skill文件存在或测试全绿。未达到时明确剩余工作，不改口径宣布完成。**
